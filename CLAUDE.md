# CLAUDE.md — 포상 진행 프로그램 프로젝트

> 이 문서는 Claude(Cowork)가 이 프로젝트에서 일할 때 따라야 할 작업 지침입니다.
> 모든 대화는 한국어로 진행하며, 본 문서의 규칙이 일반 기본 동작보다 우선합니다.

---

## 1. 프로젝트 개요

- **목표**: 2026-05-27(수) 발표회에서 사용할 **웹 기반 포상 진행 시스템**을 5/21~5/26 사이에 개발·배포·리허설까지 완료한다.
- **배포 일정**: 2026-05-26(화) 리허설, **2026-05-27(수) 본 행사**.
- **사용 환경**: 사내 발표장에서 진행자 PC(대형 스크린 송출) + 심사위원/청중 모바일 동시 접속.

### 포상 구성

| 구분 | 상 이름 | 상금 |
|---|---|---|
| 1위 | - | 100만원 |
| 2위 | - | 50만원 |
| 3위 | - | 30만원 |
| 특별상 | 협력상 | 10만원 |
| 특별상 | 도전상 | 10만원 |
| 특별상 | 토큰상 | 10만원 |
| 특별상 | 공감상 | 10만원 |

### 심사 기준 (100점 만점)

- ROI / 기대효과 (40점) — 반복성, 투입공수 절감 가치
- 업무효율화 (30점) — 워크플로우 폭 개선
- 과제 완성도 (30점)

### 심사위원 5명 & 담당 특별상

| 심사위원 | 1~3위 점수 입력 | 담당 특별상 |
|---|---|---|
| 강상우 | O | 협력상 |
| 김형욱 | O | 도전상 |
| Goos | O | 토큰상 |
| 이동옥 | O | (없음) |
| 정향모 | O | (없음) |

공감상은 **차석한 전원(청중 포함)** 1인 1표 투표로 결정한다.

---

## 2. 핵심 비즈니스 로직 (변경 금지)

1. **심사위원 5명 전원**이 약 20개 발표팀 **모두**에 대해 ROI/효율화/완성도 점수를 입력한다.
2. 팀 최종 점수 = **5명 점수의 단순 합산** (최대 500점).
3. 1~3위 = 합산 점수 내림차순 상위 3팀.
4. 동점자 처리 = **ROI 점수 합산**이 높은 팀이 우선.
5. 특별상 4종은 **1~3위에 들지 못한 팀** 중에서 선정:
   - 협력상 = 강상우 선정 / 도전상 = 김형욱 선정 / 토큰상 = Goos 선정
   - 공감상 = 청중 투표 최다 득표 팀
6. 같은 팀이 두 개 이상의 특별상을 동시 수상하지 못한다 (충돌 시 진행자에게 alert + 차순위 추천).
7. 결과 공개 순서 = **특별상 4종 → 3위 → 2위 → 1위** (드라마틱 단계별).

---

## 3. 기술 스택 (확정)

- **프레임워크**: Next.js (App Router) + TypeScript
- **스타일**: Tailwind CSS
- **백엔드/DB**: Supabase (PostgreSQL + Realtime + Auth 미사용)
- **배포**: Vercel (서버리스)
- **실시간 동기화**: Supabase Realtime (점수 입력 → 진행자/결과 화면 즉시 반영)
- **중복 투표 방지**: 브라우저 fingerprint (공감상 1인 1표)

---

## 4. 데이터 모델 (Supabase 스키마)

```sql
-- 발표팀
teams (id, name, presentation_order, status)
  -- status: '대기' | '발표중' | '발표완료'

-- 심사위원 (사전 입력)
judges (id, name, special_award_type)
  -- special_award_type: '협력상' | '도전상' | '토큰상' | NULL

-- 1~3위 산정용 점수 (5명 × 20팀 = 최대 100행)
scores (
  judge_id, team_id,
  roi_score(0-40), efficiency_score(0-30), completion_score(0-30),
  total_score, updated_at,
  UNIQUE(judge_id, team_id)  -- UPSERT
)

-- 심사위원의 특별상 선정
special_votes (judge_id, team_id, award_type)

-- 청중 공감상 투표
empathy_votes (voter_fingerprint, team_id, voted_at,
               UNIQUE(voter_fingerprint))

-- 전역 상태 (진행자가 제어)
settings (
  active_team_id, scoring_open, special_voting_open,
  empathy_voting_open, reveal_stage(0-5)
)
```

---

## 5. 개발 일정 (6일 압축)

| 일자 | Phase | 작업 |
|---|---|---|
| 5/21 | 1 | Supabase 세팅, 스키마 SQL, 심사위원/팀 사전 등록 |
| 5/22~23 | 2 | 심사위원 모바일 점수 입력 (슬라이더 + 팀 리스트) |
| 5/23~24 | 3 | 진행자 관리 대시보드 + 실시간 모니터링 |
| 5/24 | 4 | 특별상/공감상 투표 페이지 |
| 5/25 | 5 | 결과 발표 화면 (드라마틱 애니메이션) |
| 5/26 오전 | 6 | Vercel 배포 + 사내 네트워크 테스트 |
| 5/26 오후 | 7 | 리허설 + 최종 점검 |
| **5/27** | - | **본 행사** |

---

## 6. 작업 시 규칙 (Claude 행동 지침)

### 6.1 코드 작성 규칙 (HARD)

- 모든 코드는 **TypeScript**로 작성. `any` 남용 금지.
- React 컴포넌트는 함수형 + Hook. 클래스 컴포넌트 금지.
- 데이터 페칭은 가능한 한 Server Component / Server Action 우선, 클라이언트 페칭은 실시간 동기화가 필요한 경우에만.
- Supabase 호출은 `lib/supabase/` 아래 모듈로 캡슐화. 컴포넌트에서 직접 SQL 작성 금지.
- 환경변수는 `.env.local`에 저장하고 절대 커밋하지 않는다.
- 파일명은 짧게 (Windows MAX_PATH 260자 제한 고려).

### 6.2 UI/UX 규칙

- **모바일 우선 설계**. 심사위원/청중은 모바일에서 접속한다.
- 슬라이더는 큰 터치 영역 + 숫자 즉시 표시. 0.5점 단위 X, **정수 단위만**.
- 진행자 대시보드는 **태블릿~데스크탑** 화면 기준.
- 결과 발표 화면은 **1920×1080 풀스크린** 기준. 폰트 매우 크게.
- 색감: 사내 행사이므로 너무 화려하지 않게, 단 1위 발표 순간만은 컨페티/하이라이트 허용.

### 6.3 문서 산출물 규칙 (HARD)

코드 외 산출물은 Claude 기본 artifacts 대신 **반드시 moai 스킬을 사용**한다:

| 산출물 | 사용 스킬 |
|---|---|
| 진행 시나리오 (Word) | `moai-office:docx-generator` |
| 운영 매뉴얼 / 발표용 자료 | `moai-office:pptx-designer` |
| 발표팀/심사위원 명단 | `moai-office:xlsx-creator` |
| 사내 공유용 한글 공문 | `moai-office:hwpx-writer` |

**AI 슬롭 검수 (HARD)**: 사내 공유될 모든 텍스트 산출물(공지, 안내 메시지, 시나리오, 사후 리포트)의 마지막 단계에 `moai:ai-slop-reviewer`를 호출한다.
- 제외: **코드, SQL, JSON 데이터, 단순 숫자/표**는 ai-slop 검수 대상이 아니다.

### 6.4 작업 흐름 (Interview → Plan → Confirm → Execute)

비자명한 작업은 다음 4단계로 진행:

1. **Interview**: 모르는 맥락이 있으면 AskUserQuestion으로 최대 3개까지 묻는다.
2. **Plan**: 작업 계획을 짧게 정리해 사용자에게 보여준다.
3. **Confirm**: 진행 여부 확인 후 실행.
4. **Execute**: TaskCreate/TaskUpdate로 진행상황을 추적하고, 단계별 결과를 보고한다.

**스킵 조건**: 단순 조회, 사용자가 이미 상세히 지시했거나 "빠르게" 명시한 경우, 직전 작업의 자연스러운 후속.

### 6.5 Windows 환경 주의사항

- 파일명은 **짧은 한글 또는 영문 권장**.
- 산출물은 워크스페이스 폴더(`포상진행 프로그램 만들기 프로젝트`)에 저장하고 `computer://` 링크로 안내한다.
- 경로가 너무 길어 파일이 안 열린다는 사용자 보고가 오면, 짧은 경로로 복사하는 안내를 함께 제공한다.

---

## 7. 산출물별 스킬 체인 (실행 파이프라인)

### 코드 산출물 (대부분의 작업)

```
요청 예시: "심사위원 점수 입력 페이지 만들어줘"
체인: Claude 직접 작성 (TS/TSX/SQL)
입력: 명세 + 데이터 모델
출력: 파일 (.tsx, .ts, .sql) — 워크스페이스에 저장
제외 조건: ai-slop-reviewer 사용 안 함 (코드 대상 아님)
```

### 발표회 진행 시나리오 (Word)

```
요청 예시: "5/27 행사 진행 시나리오 정리해서 워드로 만들어줘"
체인: (구두 정리) → moai-office:docx-generator → moai:ai-slop-reviewer
입력: 시간표 + 멘트 흐름
출력: .docx (사회자 큐시트 포함)
```

### 운영 매뉴얼 / 심사위원 안내 (PPT)

```
요청 예시: "심사위원용 시스템 사용법 슬라이드 만들어줘"
체인: moai-office:pptx-designer → moai:ai-slop-reviewer
입력: 화면 캡처 + 단계별 설명
출력: .pptx
```

### 발표팀/심사위원 명단 (Excel)

```
요청 예시: "발표팀 명단 엑셀로 정리해줘 / 점수 결과 엑셀로 뽑아줘"
체인: moai-office:xlsx-creator
입력: 팀명/순서/심사위원 정보 또는 DB 점수 데이터
출력: .xlsx
제외 조건: 데이터/표는 ai-slop 검수 생략
```

### 사후 결과 리포트 (Word)

```
요청 예시: "행사 끝나고 결과 정리 리포트 만들어줘"
체인: (DB 집계) → moai-office:docx-generator → moai:ai-slop-reviewer
입력: scores/special_votes/empathy_votes 집계 결과
출력: .docx (상장 명단, 점수 분포, 베스트 모먼트)
```

### 사내 공지 메시지

```
요청 예시: "심사위원들에게 보낼 안내 메시지 써줘"
체인: (초안) → moai:ai-slop-reviewer
입력: 핵심 안내 사항
출력: 자연스러운 톤의 텍스트 (Slack/메일/카톡 공유용)
```

---

## 8. 사전 준비 체크리스트 (사용자 작업)

다음은 동엽님이 직접 진행해야 하는 항목입니다:

- [ ] Supabase 계정 생성 (https://supabase.com)
- [ ] Vercel 계정 생성 (https://vercel.com)
- [ ] GitHub 계정 (코드 저장용)
- [ ] 발표팀 명단 + 발표 순서 확정
- [ ] 사내 Wi-Fi에서 Vercel/Supabase 접속 가능 여부 사전 테스트
- [ ] 발표회장 대형 스크린 해상도 확인 (1920×1080 가정)
- [ ] 백업 플랜: 시스템 장애 시 사용할 엑셀 수기 시트 준비

---

## 9. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 사내 Wi-Fi에서 외부 서비스 차단 | 사전 테스트 + 모바일 데이터 폴백 안내 |
| 심사위원이 점수 입력 누락 | 진행자 대시보드에서 "5명 중 N명 입력" 실시간 표시, 독려 |
| 공감상 중복 투표 | fingerprint + Supabase UNIQUE 제약 |
| 현장 인터넷 장애 | 엑셀 수기 입력 백업 시트 |
| 발표회 당일 버그 | 5/26 리허설 필수, 5/27 오전 최종 점검 |

---

## 10. 명령어 참고

| 트리거 | 동작 |
|---|---|
| "스키마 만들어줘" | Supabase SQL 작성 → 워크스페이스 저장 |
| "점수 입력 페이지 만들어줘" | Next.js 페이지/컴포넌트 작성 |
| "진행 시나리오 정리해줘" | docx-generator → ai-slop-reviewer 체인 실행 |
| "결과 리포트 만들어줘" | 집계 → docx-generator → ai-slop-reviewer |
| "발표팀 명단 엑셀로" | xlsx-creator로 .xlsx 생성 |
| "리허설 체크리스트 줘" | 워크스페이스에 .md로 정리 |

---

_본 문서는 프로젝트 진행 중 변경될 수 있습니다. 변경 시 사용자 승인 후 직접 수정합니다._
